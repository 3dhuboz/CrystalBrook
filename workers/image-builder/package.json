{
  "name": "crystalbrook-image-builder",
  "version": "1.0.0",
  "description": "Cloudflare Worker that powers the Image Builder in the Crystal Brook admin — generates product photos via Workers AI (FLUX 1 Schnell).",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
